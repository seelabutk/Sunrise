"use strict";

exports.__esModule = true;
exports.default = getUniqueFieldNames;

function getUniqueFieldNames(objects) {
  const fields = new Set();
  objects.forEach(o => {
    Object.keys(o).forEach(field => {
      fields.add(field);
    });
  });
  return fields;
}